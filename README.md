(тут все просто, два файла в одном просто конфиги серверов типа - vless:/блабла бла бла, каждый новый конфиг с новой строки
в другом  тоже самое только перекодировано в base64(как оказалось универсальный формат для клиентов vpn)

для добавления сервака, качаем себе на комп файл с кофигами, добавляем ваш новый конфиг с новой строки, 
запускаем powershell(win+x - powershell от администратора) в него вставляем - 

$content = Get-Content (указать без скобок путь до файла с конфигами, к примеру c:\vpn_config.txt )\vpn_configs.txt -Raw
$base64 = [Convert]::ToBase64String([Text.Encoding]::UTF8.GetBytes($content))
Set-Content (указать путь где сохранить)\vpnall.txt $base64

Далее два варианта: 
а)либо тут удалить текущии версии файлов, после залить новые вами правленые 
б)либо скопировать весть текст внутри файлов и вставить в текущии.

обновить под письки впн, не забываем придумывать прикольные имена на ваши конфиги, можно писать и по ру)) - это уже старье, 
 -- -- - - новая логика ниже, делать только что ниже

 Открыл слева файл vpn_configs.txt, справа на карандаш(редактировать) и сразу там же справа вместо Soft wrap - No wrap, 
 добавляем наш конфиг в новую строку, Commit changes...(зеленная которая) и всё, готово)))
 ждем 30 сек, обновляем подписку. Больше ничего не делаем))
